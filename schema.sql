CREATE DATABASE IF NOT EXISTS gaming_db;
USE gaming_db;
-- Note: The Spring Boot application is configured to auto-generate tables (ddl-auto=update).
-- However, running the app once will generate the following schema:
-- [You can copy the CREATE TABLE statements from your console logs here after running the app once]